import { DEFAULT_OPTIONS } from "../configs/options.js";
/**
 * Função para ver as opções pre-definidas para a geração de chaves internamente.
 *
 * @function defaultOptions
 */
export const defaultOptions = () => {
  return DEFAULT_OPTIONS;
};
